<nav class="navbar navbar-expand-lg navbar-light mb-5" style="background-color: #c4e4ff;">
  <div class="container">


    <a class="navbar-brand fw-bold" href="/admin" style="color: #3773d4;">EASYKART ADMIN PANEL</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <div class="nav-item dropdown ms-auto">
        <button class="btn btn-secondary dropdown-toggle rounded-0" role="button" id="dropdownMenuButton"
          data-bs-toggle="dropdown" aria-expanded="false">
          {{#if admin}}
          {{#if adminData}}
          {{adminData.username}}
          {{else}}
          Account
          {{/if}}
          {{/if}}
        </button>
        <div class="dropdown-menu rounded-0" aria-labelledby="dropdownMenuButton">
          {{#if admin}}
          {{#if adminData}}
          <a class="dropdown-item" href="#">My Profile</a>
          <a class="dropdown-item" href="/admin/admin-logout">Logout</a>
          {{else}}
          <a class="dropdown-item" href="/admin">Login</a>
          <a class="dropdown-item" href="/admin/admin-signup">Signup</a>
          {{/if}}
          {{/if}}
        </div>
      </div>
    </div>


  </div>
</nav>